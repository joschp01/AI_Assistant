Developing the "wake up" module:

Created a venv within 1_wake_up, named "wakeUp_venv"

I installed ovos-core using:

    pip install ovos-core

I ran into some version problems and had to resolve:

    langcodes:

    pip install langcodes=3.0.0

